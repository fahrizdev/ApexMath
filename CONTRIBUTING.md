# Contributing

Thanks for improving Scalar.

## Development workflow

1. Create a focused branch from `main`.
2. Install dependencies with `npm install`.
3. Make one coherent change.
4. Run `npm test`.
5. Open a pull request explaining the problem, approach, and validation.

Keep pull requests small where practical. For behavior changes, include reproduction steps and describe the expected user experience. Never commit secrets or real user data.
